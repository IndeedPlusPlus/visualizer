h3
  // START Language list
  .pull-right
    .btn-group(dropdown='', is-open='language.listIsOpen')
      button.btn.btn-default(type='button', dropdown-toggle="")
        | {{language.selected}}
        span.caret
      ul.dropdown-menu.dropdown-menu-right.animated.fadeInUpShort(role='menu')
        li(ng-repeat='(localeId, langName) in language.available')
          a(ng-click='language.set(localeId, $event)') {{langName}}
  // END Language list

  | Welcome
  small {{ 'dashboard.WELCOME' | translate:{ appName: app.name } }}  !

.row
  .col-xs-12.text-center
    h2.text-thin Hello there!
    p This project is named Visualizer, standing for a data visualization implement.
      br
      | Please don't worry, other features will be imported from my previous when needed.
